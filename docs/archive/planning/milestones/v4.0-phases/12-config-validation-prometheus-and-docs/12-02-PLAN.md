---
phase: 12-config-validation-prometheus-and-docs
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - /Users/fjacquet/Projects/cee-exporter/.planning/ROADMAP.md
  - /Users/fjacquet/Projects/cee-exporter/.planning/STATE.md
autonomous: true
requirements:
  - FLUSH-03
  - CFG-01
  - CFG-02
  - CFG-03

must_haves:
  truths:
    - "make test passes with all 9 packages green including new metrics and validation tests"
    - "make build produces a Linux static binary without errors"
    - "make build-windows produces a Windows binary without errors"
    - "go-evtx tests pass with zero races (go test -race ./...)"
    - "ROADMAP.md Phase 12 plan list is updated to reflect 2 completed plans"
    - "STATE.md reflects v4.0 milestone as complete"
  artifacts:
    - path: "/Users/fjacquet/Projects/cee-exporter/.planning/ROADMAP.md"
      provides: "Phase 12 plan list with 2 plans marked complete"
      contains: "12-01-PLAN.md"
    - path: "/Users/fjacquet/Projects/cee-exporter/.planning/STATE.md"
      provides: "Updated project state reflecting Phase 12 completion and v4.0 milestone"
      contains: "v4.0"
  key_links:
    - from: "make test"
      to: "TestStore_LastFsyncUnix, TestMetricsHandler_AllRequiredMetrics, TestValidateOutputConfig"
      via: "go test ./..."
      pattern: "ok.*pkg/metrics|pkg/prometheus|cmd/cee-exporter"
---

<objective>
Integration gate for Phase 12: verify the full test suite passes with all new tests green across all packages, confirm both Linux and Windows builds succeed, check go-evtx tests are race-clean, and finalize planning artifacts (ROADMAP + STATE) to mark v4.0 complete.

Purpose: This is the quality gate that confirms all four requirements (FLUSH-03, CFG-01, CFG-02, CFG-03) are integrated and do not break existing functionality. It also closes out the v4.0 milestone in the planning documents.

Output:
- Confirmed green test suite (all 9 cee-exporter packages + go-evtx tests)
- Confirmed clean Linux and Windows builds
- Updated ROADMAP.md with Phase 12 plan list
- Updated STATE.md marking v4.0 complete
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/fjacquet/Projects/cee-exporter/.planning/PROJECT.md
@/Users/fjacquet/Projects/cee-exporter/.planning/ROADMAP.md
@/Users/fjacquet/Projects/cee-exporter/.planning/STATE.md
@/Users/fjacquet/Projects/cee-exporter/.planning/phases/12-config-validation-prometheus-and-docs/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full integration test — all packages, both builds, go-evtx race check</name>
  <files></files>
  <action>
    Run the complete test and build suite in sequence. Fix any failures before proceeding.

    **Step 1: cee-exporter full test suite**
    ```bash
    cd /Users/fjacquet/Projects/cee-exporter && make test
    ```
    Expected: all 9 packages pass. If any fail, diagnose and fix in the relevant source file.

    **Step 2: Linux static build**
    ```bash
    cd /Users/fjacquet/Projects/cee-exporter && make build
    ```
    Expected: ./cee-exporter binary produced, no compilation errors, CGO_ENABLED=0.

    **Step 3: Windows cross-build**
    ```bash
    cd /Users/fjacquet/Projects/cee-exporter && make build-windows
    ```
    Expected: ./cee-exporter.exe binary produced, no compilation errors.

    **Step 4: go-evtx race detector**
    ```bash
    cd /Users/fjacquet/Projects/go-evtx && go test -race ./... -count=1
    ```
    Expected: all tests pass, zero races reported (including any OnFsync callback concurrency).

    **Step 5: Spot-check Prometheus gauge registration**
    ```bash
    cd /Users/fjacquet/Projects/cee-exporter && go test ./pkg/prometheus/ -v -run TestMetricsHandler_AllRequiredMetrics -count=1
    ```
    Expected: output contains "cee_last_fsync_unix_seconds" line and test PASS.

    **Step 6: Spot-check validation function**
    ```bash
    cd /Users/fjacquet/Projects/cee-exporter && go test ./cmd/cee-exporter/ -v -run TestValidateOutputConfig -count=1
    ```
    Expected: all 8 subtests PASS.

    If any step fails:
    - Compilation error: fix the import or syntax in the relevant file; re-run
    - Test failure: read the failure message, fix the implementation or test, re-run
    - Race condition: examine the OnFsync callback — ensure it is called while NOT holding w.mu (the callback is invoked inside flushChunkLocked/tickFlushLocked which already hold w.mu; the callback itself must not re-acquire w.mu)

    Commit any fixes with: `git commit -m "fix(12-02): integration fixes from full build verification"`
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cee-exporter && make test && make build && make build-windows</automated>
  </verify>
  <done>
    - make test exits 0 with all packages green (including pkg/metrics, pkg/prometheus, cmd/cee-exporter new tests)
    - make build produces ./cee-exporter without errors
    - make build-windows produces ./cee-exporter.exe without errors
    - go test -race ./... in go-evtx passes with zero races
    - TestMetricsHandler_AllRequiredMetrics passes with cee_last_fsync_unix_seconds present
    - TestValidateOutputConfig passes all 8 cases
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md and STATE.md to mark v4.0 complete</name>
  <files>
    /Users/fjacquet/Projects/cee-exporter/.planning/ROADMAP.md,
    /Users/fjacquet/Projects/cee-exporter/.planning/STATE.md
  </files>
  <action>
    **ROADMAP.md updates:**

    1. Find the Phase 12 entry. Update:
       - `**Plans:** TBD` → `**Plans:** 2 plans`
       - Add the plan list:
         ```
         Plans:
         - [x] 12-01-PLAN.md — Add OnFsync callback (go-evtx v0.5.0), fsync Prometheus gauge, validateOutputConfig, config documentation (FLUSH-03, CFG-01, CFG-02, CFG-03)
         - [x] 12-02-PLAN.md — Full integration test suite, build verification, planning artifact finalization
         ```
       - Update the progress table row for Phase 12:
         `| 12. Config, Validation, Prometheus and Docs | v4.0 | 0/? | Not started | - |`
         →
         `| 12. Config, Validation, Prometheus and Docs | v4.0 | 2/2 | Complete | 2026-03-05 |`

    2. Update the v4.0 milestone status:
       - Change `🚧 **v4.0 Industrialisation** — Phases 9-12 (in progress)`
         to `✅ **v4.0 Industrialisation** — Phases 9-12 (shipped 2026-03-05)`
       - Move v4.0 phases into a `<details>` collapsible block similar to v1.0/v2.0/v3.0.

    **STATE.md updates:**

    Update the following fields:
    - `status: executing` → `status: complete`
    - `stopped_at:` → `stopped_at: Completed 12-02-PLAN.md — v4.0 Industrialisation milestone complete; all FLUSH-*, CFG-* requirements satisfied`
    - `last_activity:` → `last_activity: 2026-03-05 — Phase 12 complete; go-evtx v0.5.0 (OnFsync callback); cee_last_fsync_unix_seconds Prometheus gauge; validateOutputConfig startup validation; config.toml and config.toml.example updated`
    - Update progress: `completed_phases: 5` (all 5 v4.0 phases done), `completed_plans: 10`, `percent: 100`
    - Update `Current Position` section: Phase 12 of 12 — complete
    - Add to Decisions: `[Phase 12]: OnFsync callback in RotationConfig (Option A from research); go-evtx v0.5.0; validateOutputConfig rejects flush_interval_s=0 when type=evtx; config.toml shows active values, config.toml.example shows commented documentation`

    Commit: `git add .planning/ROADMAP.md .planning/STATE.md && git commit -m "docs(12-02): mark v4.0 Industrialisation milestone complete"`
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cee-exporter && grep "12-01-PLAN.md" .planning/ROADMAP.md && grep "v4.0.*shipped" .planning/ROADMAP.md && grep "complete" .planning/STATE.md | head -3</automated>
  </verify>
  <done>
    - ROADMAP.md Phase 12 has 2/2 plans marked complete with plan list
    - ROADMAP.md v4.0 milestone status shows shipped 2026-03-05
    - STATE.md status is complete with correct stopped_at and last_activity
    - Git history shows the final commit for Phase 12
  </done>
</task>

</tasks>

<verification>
Phase 12 is complete when:

1. `cd /Users/fjacquet/Projects/cee-exporter && make test` — all packages green
2. `cd /Users/fjacquet/Projects/cee-exporter && make build` — Linux binary produced
3. `cd /Users/fjacquet/Projects/cee-exporter && make build-windows` — Windows binary produced
4. `cd /Users/fjacquet/Projects/go-evtx && go test -race ./... -count=1` — zero races
5. `grep cee_last_fsync_unix_seconds /Users/fjacquet/Projects/cee-exporter/pkg/prometheus/handler.go` — found
6. `grep validateOutputConfig /Users/fjacquet/Projects/cee-exporter/cmd/cee-exporter/main.go` — found
7. `grep flush_interval_s /Users/fjacquet/Projects/cee-exporter/config.toml` — found (active value)
8. `grep flush_interval_s /Users/fjacquet/Projects/cee-exporter/config.toml.example` — found (commented)
9. `grep "v4.0.*shipped" /Users/fjacquet/Projects/cee-exporter/.planning/ROADMAP.md` — found
</verification>

<success_criteria>
- All cee-exporter packages pass make test (including 3 new test functions)
- Both platform builds succeed (CGO_ENABLED=0)
- go-evtx race detector confirms zero races with OnFsync callback
- ROADMAP.md shows v4.0 as shipped with Phase 12 plans listed
- STATE.md reflects v4.0 complete status
- v4.0 Industrialisation milestone is fully delivered: go-evtx OSS module + durability + rotation + observability + config validation
</success_criteria>

<output>
After completion, create `/Users/fjacquet/Projects/cee-exporter/.planning/phases/12-config-validation-prometheus-and-docs/12-02-SUMMARY.md`
</output>
