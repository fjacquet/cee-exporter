# Promises

Every user-facing claim this project makes, and the check that keeps it
true. A claim with no verifying job is either deleted or labelled
**Unverified** — it is never left to look proven. A claim checked only by a
human running a command by hand, not by CI, is labelled **Verified (manual)**
naming that step. A claim where part of it is genuinely tested and part is
not is labelled **Verified (partial)**, with the "Verified by" column stating
exactly which part.

New claims arrive with their job. That is the rule this file exists to
enforce. Claims below are primarily drawn from `README.md`, `docs/index.md`,
and `docs/PRD.md` (including its Success Metrics section) as they stood on
2026-08-08 — but the "Where stated" column names the actual source file for
each claim, which for several rows turned out to be `CLAUDE.md`,
`docs/operator-guide.md`, `docs/requirements.md`, or `CHANGELOG.md` instead,
once each attribution was checked against the real file content rather than
assumed. Every "Verified by" test name below was opened and read before
being cited here — see `docs/requirements.md` for the fuller per-requirement
traceability this table draws on.

## CEPA protocol

| Claim | Where stated | Verified by | Status |
|---|---|---|---|
| RegisterRequest handshake returns HTTP 200 with a strictly empty body | `README.md` (states the handshake exists, not the empty-body detail); the precise wording is `CLAUDE.md` ("must respond HTTP 200 with strictly empty body — any XML body is a fatal CEPA error") and `docs/requirements.md` CEPA-01 | `pkg/server/server_test.go::TestServeHTTP_RegisterRequest_EmptyBody` asserts the response body is exactly zero bytes, not merely visually empty (CI `ci` job, `make test`) | Verified |
| The handler ACKs the heartbeat within the 3-second CEPA budget | `README.md`, `docs/PRD.md` NFR | The handler ACKs without waiting for the writer; verified by `TestServeHTTP_ACKsBeforeQueueWork`, which proves `ServeHTTP` returns a written 200 while the queued write is provably still in progress. `TestServeHTTP_LargeBatchACKsWellUnder3s` additionally asserts a 2000-event VCAPS batch ACKs in under 2s (the test's own doc comment measures ~233ms under `-race` locally; re-run for this review at 0.19s), well inside the 3s limit (CI `ci` job, `make test`) | Verified |
| Malformed/non-RegisterRequest bodies still get a 200, not a 4xx/5xx (CEPA must never be told the endpoint is unreachable) | Not stated in any user-facing doc — this is an internal reliability guarantee documented only as a code comment (`pkg/server/server.go:80`, "Still ACK so CEPA doesn't mark us unreachable"). Included here because it is a core CEPA reliability property, not because README/index.md assert it | `pkg/server/server_test.go::TestServeHTTP_ParseErrorStillACKs` | Verified |
| VCAPS bulk batch payloads (`<EventBatch>`) are parsed | `docs/PRD.md` CEPA-04 (by ID only — the detail is in `CLAUDE.md`: "Handles both single-event and VCAPS batch (`<EventBatch>`) payloads") | `pkg/parser/parser_test.go::TestParse` ("vcaps_batch_two_events" case) | Verified |
| Heartbeat latency is bounded, not just "ACK ordering is correct" (CEPA-02) | `docs/PRD.md` NFR | Nothing measures wall-clock response latency against the 3s limit directly — the ordering guarantee above is tested, not the latency number itself | **Unverified** — do not treat CEPA-02 as closed by the ACK-ordering tests above; see `docs/requirements.md` |
| CEPA event types map to the correct Windows EventID and access mask (`CEPP_CREATE_FILE`/`CEPP_CREATE_DIRECTORY`/`CEPP_FILE_READ`/`CEPP_FILE_WRITE`/`CEPP_CLOSE_MODIFIED` → 4663; `CEPP_DELETE_FILE`/`CEPP_DELETE_DIRECTORY` → 4660; `CEPP_SETACL_FILE`/`CEPP_SETACL_DIRECTORY` → 4670) — the semantic core of the product | `README.md` (CEPA protocol compliance bullet, implicitly); `docs/requirements.md` MAP-01..MAP-06 (six requirement IDs, one per event-type family) | `pkg/mapper/mapper_test.go::TestMapEventID` table-drives nine concrete `CEPP_*` types plus an unknown-type fallback (not "six" — that was the requirement-ID count, not the test's case count) asserting both `EventID` and `AccessMask` for each; `TestMapFieldPropagation` covers field propagation, e.g. I/O stats on `CEPP_CLOSE_MODIFIED` | Verified |
| The CEPA client sends events over plain HTTP only — always register `http://`, never `https://`, in PowerStore | `docs/index.md`, `docs/PRD.md` "Protocol Constraint" | Nothing runs an actual PowerStore CEPA client against this daemon; this is a protocol fact about Dell's implementation, external to this codebase, not something a test in this repo can exercise. Correct by design: `pkg/server` never requires TLS and the operator guide's TLS section repeats the same warning | **Unverified** — external protocol behaviour, not verifiable from this repo; documented consistently is the best available assurance |

## Output backends

| Claim | Where stated | Verified by | Status |
|---|---|---|---|
| GELF 1.1 JSON output over UDP or TCP → Graylog | `README.md`, `docs/index.md` | `pkg/evtx/writer_gelf_test.go` verifies payload construction and GELF 1.1 field validity only — no test dials a real UDP or TCP socket, and `writer_helpers_test.go`'s `sendWithRetry` tests use a fake closure, not `GELFWriter.connect()`/`send()` | **Verified (partial)** — message format is verified; on-the-wire UDP/TCP delivery is not |
| RFC 5424 syslog output over UDP or TCP | `README.md`, `docs/index.md` | `pkg/evtx/writer_syslog_test.go::TestBuildSyslog5424` (RFC 5424 message format). Before v5.0.1 it asserted bare substrings and named only four of the seven `audit@32473` SD-PARAMs, so deleting `Domain`, `AccessMask` or `ClientAddr` left it green under a subtest called "all required fields present"; it now asserts each rendered `Key="Value"` pair plus the header fields, and deleting any one `AddDatum` fails it. `TestBuildSyslog5424ProcID` covers the PROCID nil-value branch. `TestSyslogTCPFraming` verifies RFC 6587 octet-counting framing against a real `net.Conn` via `net.Pipe`, exercising the writer's actual `send()` method | **Verified (partial)** — TCP framing is exercised against a real connection; UDP is checked for message format only, not dialed |
| Beats/Lumberjack v2 output to Logstash/Graylog, with optional TLS | `README.md`, `docs/index.md` | `pkg/evtx/writer_beats_test.go::TestBuildBeatsEvent` (event field mapping) and `TestBeatsWriterDialerInjection`. Before v5.0.1 the latter proved nothing about TLS: both its cases dialled an unreachable port, where the TCP connect is refused before any handshake, so they returned the identical error and deleting the entire TLS branch left the test green. It now runs against a real listener — plain TCP must connect, `TLS: true` must fail at the TLS layer rather than silently succeed in the clear, and an already-cancelled context must abort the dial — each verified by mutation | **Verified (partial)** — event mapping is verified, and the TLS branch is now proven to be taken rather than merely configured; still, no test completes a TLS handshake against a real Beats server or performs a successful Lumberjack send |
| Native `.evtx` files written on non-Windows platforms | `README.md`, `docs/index.md`, `docs/PRD.md` OUT-05 | `pkg/evtx/writer_evtx_notwindows_test.go` (`TestBinaryEvtxWriter_WriteClose`, `TestBinaryEvtxWriter_ChunkLayout`, and others) | Verified |
| Generated `.evtx` files open correctly in Windows Event Viewer and parse with forensics tools | `docs/PRD.md` OUT-06, Success Metrics | The `evtx-readback` job in `.github/workflows/ci.yml` downloads a `.evtx` generated by the Linux `evtx-oracle` job and reads it on `windows-latest` with `Get-WinEvent -Path`, asserting 3 records, the event ID set {4660, 4663, 4670}, `ToXml()` per record, and `ObjectName` in the rendered XML. `evtx-oracle` additionally checks the file with python-evtx (`tools/evtx-debug/verify_evtx.py`) — an independent parser, which is the "forensics tools" half, though it is measurably lenient: it parses the pre-v0.7.0 files `Get-WinEvent` rejects as `The event log file is corrupted`. What none of that covers: description rendering. Measured 2026-08-10 on winvm (Windows Server 2025 Datacenter), in a session where the event source was confirmed registered — the same session's live Application log rendered all three descriptions correctly — a `.evtx` copied from Linux and opened with `Get-WinEvent -Path` still enumerates all three records with `LogName=[]` and `Message: <null>`. Not the registration trap, since the live log worked in the same session; stated hypothesis is that these records carry an empty `Channel`, so `LogName` resolves empty and Windows cannot bind a saved-log record to the registered provider. Confirming or fixing that would mean changing go-evtx, out of scope here. The Event Viewer GUI open/placement question (does it open, where do events land) was not run — it needs an interactive desktop session, unavailable on `winvm` over its SSH-only connection — see [docs/windows-verification.md](windows-verification.md) section 5 | **Verified (partial)** — `Get-WinEvent` read-back (file opens, 3 records, the event ID set, `ToXml()`, `ObjectName`) is CI-gated on every push; description rendering from a saved log is measured not to work even with the source registered, recorded as a known limitation rather than pursued further; the Event Viewer GUI open/placement question is manual and not yet run, dated 2026-08-10 |
| Win32 EventLog writer registers a "PowerStore-CEPA" source against the exporter's own executable and writes via `ReportEvent` | `README.md`/`docs/index.md` state only the `ReportEvent` writing; the "PowerStore-CEPA" source name is documented only in `CHANGELOG.md`'s `[1.0.0]` entry and the code itself (`win32SourceName`); "against the exporter's own executable" is `pkg/evtx/writer_windows.go`'s package comment, rewritten for v5.0 | `pkg/evtx/writer_windows_test.go::TestEnsureEventSource_PointsAtCurrentExecutable` and `::TestEnsureEventSource_RepointsStaleSource` — the first tests in this repo's history to execute `writer_windows.go` at all — run in the `windows` CI job (`.github/workflows/ci.yml`), confirmed green in run [31315601780](https://github.com/fjacquet/cee-exporter/actions/runs/31315601780) (`pkg/evtx ... ok`, `-race`, real `gcc`, not skipped). That job also builds the actual release binary and drives it with `-emit-test-events`, exercising `NewWin32EventLogWriter` → `ensureEventSource` → `eventlog.Install` → `ReportEvent` end to end — its "Service install/uninstall round-trip" step additionally confirms real SCM registration (`sc.exe query` returned `STATE: 1 STOPPED` for a service that did not exist before `install`, not an error) | Verified |
| Windows Event Log entries for 4660/4663/4670 render a real description in Event Viewer and other Event Log API readers, instead of the former placeholder text | `pkg/evtx/writer_windows.go`'s package comment (rewritten for v5.0); `docs/windows-verification.md` | The `windows` CI job's "Assert Event Viewer renders real text, not the placeholder" step asserts, for every one of 4660/4663/4670, that `Get-WinEvent`'s `.Message` is non-null and contains the payload body, and that `Get-EventLog`'s `.Message` does not contain the placeholder string — confirmed directly in run [31315601780](https://github.com/fjacquet/cee-exporter/actions/runs/31315601780)'s raw log (`event 4660 renders correctly`, `event 4663 renders correctly`, `event 4670 renders correctly`). That alone proves the resolution mechanism on a freshly-registered source; it does not by itself prove the *upgrade* case, since the CI runner has no prior state to upgrade from. `docs/windows-verification.md` (manual protocol, run 2026-08-09 on Windows Server 2025) closes that gap: it records the actual placeholder text (`Get-EventLog`: `The description for Event ID '4663' in Source 'PowerStore-CEPA' cannot be found. ...`; `Get-WinEvent`: `$null`) from a source genuinely registered the old, broken way, then the real post-upgrade text (`An attempt was made to access an object.` etc.) after running the v5.0 binary in place, with `win32_source_repointing` observed firing in the log — and separately confirms the registration-failure degradation path (`win32_source_registration_failed` logged, exporter still writes all three events and exits 0) under a genuine registry `ACCESS_DENIED`, though not via a truly separate non-administrator OS logon (that specific mechanic failed on the test host for reasons unrelated to this code — see that document's step 4). This claim does **not** extend to SIEM content-pack compatibility: whether a given SIEM's content pack, built to parse a genuine Windows Security-log event, treats this output as equivalent has not been demonstrated and is not asserted here | **Verified (manual)** — the core rendering mechanism is proven by the automated `windows` CI job on every push; the full before/after against a genuinely pre-v5.0-registered source, and the degradation path, were confirmed by the dated manual run in `docs/windows-verification.md`, 2026-08-09 |
| Multi-target fan-out: one backend's failure doesn't block delivery to others | `README.md`/`docs/index.md` state only that fan-out exists, not the failure-isolation guarantee; that detail is `docs/PRD.md` NFR Reliability ("failed backend does not block others (MultiWriter)") | `pkg/evtx/writer_multi_test.go::TestMultiWriter_WriteEvent_AllCalledJoinedErrors` | Verified |
| SIGHUP triggers immediate EVTX rotation on non-Windows platforms | `docs/operator-guide.md` | `cmd/cee-exporter/sighup_notwindows.go` has no dedicated test of the signal registration itself; `pkg/evtx/writer_multi_test.go::TestMultiWriter_Rotate_ForwardsAndSkips` verifies only `MultiWriter`'s fan-out of `Rotate()` to rotator/non-rotator backends — nothing asserts a `.evtx` file actually rotates on disk (no `TestBinaryEvtxWriter_Rotate`) | **Verified (partial)** — the `Rotate()` fan-out is tested; neither the SIGHUP wiring nor an actual file rotation is |
| TCP GELF/Beats connections reconnect automatically after a lost connection | `docs/PRD.md` NFR Reliability | `pkg/evtx/writer_helpers_test.go`'s `sendWithRetry` tests (`TestSendWithRetry_FirstSendSucceeds`, `TestSendWithRetry_RetrySucceeds`, `TestSendWithRetry_ReconnectFails`, `TestSendWithRetry_RetryFails`) exercise the shared retry helper with fake closures, not a real `GELFWriter`/`BeatsWriter` reconnecting over an actual dropped socket | **Verified (partial)** — the retry/reconnect logic itself is tested; a real network reconnection is not |

## TLS

| Claim | Where stated | Verified by | Status |
|---|---|---|---|
| `tls_mode="self-signed"` generates a runtime ECDSA certificate, no files or network needed | `README.md`, `docs/operator-guide.md` | `cmd/cee-exporter/tls_test.go::TestBuildSelfSignedTLS` asserts `MinVersion`, exactly one certificate, a validity window straddling now, and the expected DNS name(s) — it never inspects the key algorithm, so the "ECDSA" half of the claim is confirmed only by reading `cmd/cee-exporter/tls.go:56` (`ecdsa.GenerateKey(elliptic.P256(), …)`), not by this test | **Verified (partial)** — certificate shape and validity are tested; the key algorithm is correct by inspection only |
| `tls_mode="manual"` (or legacy `tls=true`+`cert_file`/`key_file`) loads certificates from files, and the legacy form auto-migrates to `tls_mode="manual"` | `README.md` states only that a "manual" mode exists; the legacy-field auto-migration detail is stated only in `docs/operator-guide.md` (added this release) and `config.toml.example`'s "Backward compat" comment | `cmd/cee-exporter/tls_test.go::TestMigrateListenConfig` covers config recognition and the legacy-field migration; no test drives an actual TLS handshake against the listener | **Verified (partial)** |
| `tls_mode="acme"` auto-obtains and renews a certificate from Let's Encrypt via TLS-ALPN-01 | `README.md`, `docs/operator-guide.md`, `docs/PRD.md` TLS-03 | `cmd/cee-exporter/tls_test.go::TestMigrateListenConfig` ("already_set_acme" case) covers config recognition only — nothing exercises a real ACME exchange, which needs a live ACME server | **Unverified** for the actual certificate issuance/renewal; config wiring only |
| TLS certificate expiry is surfaced via `/health` on every request, with a WARN log when fewer than 30 days remain | `README.md` | `pkg/server/health_test.go::TestHealth_TLSCertExpiryPopulated` confirms expiry is computed and exposed via `/health`. Since v5.0.1 three further tests pin the boundaries that one cannot see, because it uses a 400-day cert: `TestHealth_TLSDaysRemainingSurvivesLastDay` (a 12-hour cert, where the day count is 0 and the field must still be present), `TestHealth_TLSDaysRemainingAbsentWhenNoCert` (no cert, where it must be absent), and `TestHealth_TLSDaysRemainingNegativeWhenExpired` (already-expired certs, which `int()` truncation also reported as 0). Each mutation was run: `int` with `omitempty` fails the first, `int` without it fails the second, and `int()` in place of `math.Floor` fails the third. The `tls_cert_expiry_soon` WARN log itself (inside `buildTLSInfo`'s `days < 30` branch, run on every `/health` call — there is no separate startup check) still has no test | **Verified (partial)** — the JSON contract including the nil-vs-0 distinction is verified; the WARN log is not |

## Observability

| Claim | Where stated | Verified by | Status |
|---|---|---|---|
| `/metrics` exposes `cee_events_received_total`, `cee_events_written_total`, `cee_events_dropped_total`, `cee_events_truncated_total`, `cee_writer_errors_total`, `cee_queue_depth`, `cee_last_fsync_unix_seconds`, and `cee_build_info` | The full, named list of eight metrics is stated only in `docs/operator-guide.md`'s metrics table (completed this release). `README.md`/`docs/index.md`/`docs/PRD.md` Success Metrics only claim a Prometheus endpoint exists, not these specific names | `pkg/prometheus/handler_test.go::TestMetricsHandler_AllRequiredMetrics` (the first seven) and `TestBuildInfoMetric` (`cee_build_info` with `version`/`go_version` labels), and confirms Go runtime metrics are absent from the private registry | Verified |
| `/metrics` is served on a dedicated, configurable port (default 9228) | `README.md`, `docs/index.md` | The default is set in `cmd/cee-exporter/main.go` (`MetricsConfig.Addr = "0.0.0.0:9228"`); no test exercises the actual port binding or a non-default value end-to-end | **Verified (partial)** — metric content is tested; the port/binding itself is not |
| `GET /health` returns JSON including `queue_depth` and the `writer` block (`type`/`target`), and returns HTTP 200 on the healthy path; degradation is signalled by the `"status"` field flipping to `"degraded"`, not by the HTTP status field. The always-200 behaviour is a deliberate design decision, not a gap — see `docs/operator-guide.md`'s Health endpoint section and the "Decided against" note in the promise-remediation design doc for why a 503 would make queue-overflow incidents worse, not better | `docs/operator-guide.md` | `pkg/server/health_test.go::TestHealth_OKWithBaseline` asserts `w.Code == 200`, `status == "ok"`, `queue_depth`, and the `writer.type`/`writer.target` fields on the healthy path; `TestHealth_MethodNotAllowed` asserts a non-GET request gets 405 before any of this runs. `TestHealth_DegradedWhenDropped` asserts `status` flips to `"degraded"` but does not read `w.Code`. Neither test reads `uptime_seconds` or any `events_*_total` field — those are populated at `health.go:82-86` but not asserted by any test. That `health.go`'s `ServeHTTP` contains exactly one `WriteHeader` call on the GET path (`:48`, unconditional `http.StatusOK`) — the basis for "200 even when degraded" — is correct by direct reading of the function, not by a dedicated test | **Verified (partial)** — `queue_depth`, the `writer` block, the `status` field's healthy/degraded values, and 405-on-non-GET are test-verified; `uptime_seconds`, the event counters, and "200 even when degraded" are correct by inspection only |
| Structured JSON logging (`slog`) includes event/queue/latency fields on every batch | `README.md` states only that structured JSON logging exists; the specific field list (`event_type`, `file_path`, `client_ip`, `queue_depth`, `latency_ms`) is stated only in `docs/requirements.md` OBS-02 | Nothing captures/parses `slog` output to confirm field presence; correct by inspection of `pkg/server/server.go` | **Unverified** |
| Dropped events (queue overflow) are logged at WARN with a running total | `README.md`'s "async queue" bullet does not mention WARN logging or drop counts; the precise claim is `CLAUDE.md` ("Drops events with WARN log when full") and `docs/operator-guide.md`'s metrics table (`cee_events_dropped_total`) | `pkg/queue/queue_test.go::TestDropOnFull` verifies the counter increments; the WARN log line itself is not asserted by any test | **Verified (partial)** |
| The async queue ACKs the HTTP request immediately and processes events in the background | `README.md` | Covered by the CEPA-protocol ACK-ordering row above; queue mechanics (enqueue, drop-on-full, drain-on-stop) also covered by `pkg/queue/queue_test.go` | Verified |
| Queue capacity defaults to 100,000 events | `docs/PRD.md` NFR Throughput | `cmd/cee-exporter/main.go:158` sets `Capacity: 100000` as the default; no test asserts this specific default value (`pkg/queue`'s tests all pass an explicit capacity) | **Verified (partial)** — correct by inspection of the default; not asserted by a test |
| Binary size ~6 MB (stripped, CGO_ENABLED=0) | `docs/index.md` (previously — corrected in this release) | Measured directly for this review: `make build-linux` produces a 12,038,306-byte (~11.5 MB) binary today, not ~6 MB. The stated figure was stale, not false when written — the binary has grown as dependencies (`prometheus/client_golang`, `kardianos/service`, `golang.org/x/crypto/acme`, `github.com/fjacquet/go-evtx`, etc.) were added across v2.0–v4.0 | **Unverified — was stale**; superseded by the row below |
| Binary size ~11.5 MB (stripped, CGO_ENABLED=0, linux/amd64) | `docs/index.md` (current figure, corrected in this release) | `make build-linux` → 12,038,306 bytes, measured 2026-08-08. No CI check pins or re-measures this number on an ongoing basis, so it can drift again exactly as the retired ~6 MB figure did | **Verified (manual)** — measured directly, not asserted by any test |

## Build, packaging, and distribution

| Claim | Where stated | Verified by | Status |
|---|---|---|---|
| Requires Go 1.26.5 | `README.md`, `docs/index.md`, `docs/operator-guide.md` | `go.mod:3` (`go 1.26.5`); the Go toolchain itself refuses to build against an older `go` if `GOTOOLCHAIN=local` and the installed version is too old (verified directly for the previous `golang:1.24-alpine` builder image during Task 3 — it failed with `go: go.mod requires go >= 1.26.5`) | Verified |
| Single static binary, `CGO_ENABLED=0`, no external runtime dependencies | `README.md` | `CGO_ENABLED=0` is set on every binary-producing path (`Makefile` `build-linux`/`build-windows`/`build-darwin`, `Dockerfile`, `.goreleaser.yaml`) — a Go build with CGO disabled cannot link a dynamic dependency. No CI step runs `ldd`/`file` against a built binary to confirm this directly | **Verified (partial)** — guaranteed by build configuration; not confirmed by inspecting an actual built artifact |
| Build-stamped version reaches the running binary (`-X main.version=...`) and appears in the `cee_build_info` metric, replacing the previous hardcoded `"1.0.0"` | Not made by README.md or docs/PRD.md — corrected from an earlier draft of this row that cited both incorrectly. The actual claim is `CLAUDE.md` ("All build targets stamp the version: `-ldflags "-X main.version=$(VERSION)"`") and this release's own `CHANGELOG.md` `[4.1.3]` entry | `cmd/cee-exporter/version_test.go` (`TestVersion_DefaultIsDev`, `TestVersion_NotHardcodedRelease`) only asserts the *unstamped* default is `"dev"` and isn't a hardcoded release string — no test builds a binary with `-ldflags` and inspects it. **Verified (manual), now against a real release artifact:** the `cee-exporter_4.1.3_darwin_arm64.tar.gz` binary downloaded from the published `v4.1.3` release logs `"version":"4.1.3"` at startup and serves `cee_build_info{go_version="go1.26.5",version="4.1.3"} 1`. This supersedes the earlier locally-built check: the artifact users actually download is stamped. Still no automated job asserts this — it remains a manual step (verified 2026-08-08, post-tag) | **Verified (manual)** |
| Releases build for Linux and macOS on amd64 and arm64, and Windows on amd64 | `docs/index.md`, `docs/PRD.md` NFR Portability | `.goreleaser.yaml` build matrix, with an explicit `ignore:` for windows/arm64. `goreleaser build --snapshot` resolves to exactly five targets — linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64 — and `goreleaser check` validates the config (verified 2026-08-09). Note the `v4.1.3` release predates this change and *did* ship a windows/arm64 zip; the first release to match this row is the next tag. Windows/ARM64 was dropped deliberately: no target market (Windows Server does not ship for ARM64) and no CI runner to execute it, so it had been published without ever being run | **Verified** for the build matrix; the released artifact set matching it is **unverified** until the next tag |
| Docker image published to `ghcr.io/fjacquet/cee-exporter` (multi-arch `linux/amd64`+`linux/arm64`) as part of the release | `README.md` | `.goreleaser.yaml` `dockers_v2:` block, invoked by `release.yml` on a `v*` tag. This mechanism is new on this branch and has never run as part of a real release. Task 3 verified the `Dockerfile.goreleaser` context resolution and image runtime behaviour by hand-assembling the same `<goos>/<goarch>/<binary>` layout goreleaser uses and building/running both architectures directly — both produced a working, correctly version-stamped image. What that did not prove was goreleaser's own orchestration of `dockers_v2` (tag templating, manifest assembly, `extra_files` staging). **The `v4.1.3` tag has since run it.** `docker buildx imagetools inspect ghcr.io/fjacquet/cee-exporter:4.1.3` returns an OCI image index with `linux/amd64` and `linux/arm64` manifests, and `:latest` resolves to the same two digests. Note the published tag is `4.1.3`, without the `v` — goreleaser's `.Version` strips the prefix (verified 2026-08-08, post-tag) | Verified |
| `LICENSE` (MIT) is present at the repo root | `README.md` badge | `test -f LICENSE` for presence; its first line reads `MIT License`, so the MIT designation is checked against content rather than assumed from the filename (verified 2026-08-09) | Verified |
| `LICENSE` is present inside every release archive | Exit criteria for this release | `.goreleaser.yaml` `archives.files` lists `README.md` and `LICENSE` — but this is config, not an executed job. `c792faa` had previously *removed* `LICENSE` from the archive list because the release build failed on the missing file (`LICENSE` didn't exist in the repo at all until `313c815`, on this branch); `git show v4.1.2:.goreleaser.yaml` confirms `files:` was `README.md` only at the last real release. No release had been cut since `313c815` re-added the line. **The `v4.1.3` release has now been cut and all six archives were inspected, not a sample:** `tar tzf` on each of the four `.tar.gz` files and `unzip -l` on both `.zip` files each list exactly one `LICENSE` entry, and the extracted file's md5 matches the repo's `LICENSE` byte-for-byte (`98ccd23e70491829e5a029fdb4a3e79f`), so the archives carry the real MIT text rather than an empty placeholder (verified 2026-08-09, post-tag) | Verified |
| `cee-exporter.exe install` registers the daemon with the SCM, and the resulting service actually starts and stops | `README.md`, `docs/operator-guide.md`, `docs/PRD.md` DEPLOY-03 | The `windows` CI job's "Service install/uninstall round-trip" step, run [31318610291](https://github.com/fjacquet/cee-exporter/actions/runs/31318610291): `sc.exe query` after `install` shows the service registered, then `sc.exe start` plus polling `Get-Service` confirms it reaches `Running`, and `sc.exe stop` confirms `Stopped` — exercising `svcProgram.Start`/`Stop` in `cmd/cee-exporter/service_windows.go` directly, not just kardianos's SCM registration helpers. Every native `.exe`/`sc.exe` call in the step is gated on `$LASTEXITCODE` | **Verified** |
| `cee-exporter.exe uninstall` removes the daemon from the SCM | `README.md`, `docs/operator-guide.md`, `docs/PRD.md` DEPLOY-04 | Same step, same run: `uninstall` prints its success message and is gated on `$LASTEXITCODE`, but the step does not re-run `sc.exe query` afterward to independently confirm the service is actually gone from the SCM — removal is confirmed by the tool's own success message and exit code only | **Verified (partial)** |
| Windows Service auto-restarts after an unexpected crash | `README.md`, `docs/operator-guide.md`, `docs/PRD.md` DEPLOY-05 | The `windows` CI job proves the SCM can start and cleanly stop the service (see the row above), but nothing simulates a crash or observes the `OnFailure: "restart"` recovery action in `service_windows.go`'s `svcConfig` actually firing. Correct by inspection only | **Unverified** |
| `go test ./...` passes with zero failures | `docs/PRD.md` Success Metrics | Linux: CI `ci` job runs `make test` (`go test -race ./...`) on every push, currently green. Windows: the `windows` CI job's "Run tests" step runs the same command, confirmed using a real `gcc` (`C:\mingw64\bin\gcc.exe` on GitHub's `windows-latest` image) rather than silently degrading to the non-`-race` fallback | **Verified** on both platforms |
| Linux systemd unit starts cleanly and is self-sufficient (no manual directory setup), including the `tls_mode="acme"` default cache path | `docs/operator-guide.md`, `deploy/systemd/cee-exporter.service` | No automated CI test — verified by hand for this release: booted the unit under real `systemd` (PID 1, `DynamicUser=yes`, `ProtectSystem=strict`) in a container with `tls_mode="acme"` configured. `systemctl status` showed `active (running)`, the ACME challenge listener bound `:443`, and `cee_exporter_ready` logged. Entered the running service's own mount namespace (`nsenter --target $MAINPID --mount`) and, as its exact dynamic UID, successfully created and wrote `/var/cache/cee-exporter/acme/test.pem` — confirming `CacheDirectory=cee-exporter` makes the default `acme_cache_dir` writable with zero operator action, closing the gap Amendment A2 identified | **Verified (manual)** |

## Documentation and process

| Claim | Where stated | Verified by | Status |
|---|---|---|---|
| An operator can follow the README quickstart and see events in Graylog within 15 minutes | `docs/PRD.md` Success Metrics | Nothing — no timed or automated check of this exists | **Unverified** |
| `docs/PRD.md` links a `docs/requirements.md` that exists; the retired `.planning/` directory no longer sits at the repo root | Exit criteria for this release | `test -f docs/requirements.md`, `test -d docs/archive/planning` | Verified |
The two specific retired phantom keys — `type = "binary-evtx"` and `acme_staging` — cannot silently return to `docs/`, `README.md`, `config.toml`, or `config.toml.example` | `docs/operator-guide.md`, `config.toml.example` | `.github/workflows/docs-lint.yml` `no-phantom-config` job, run on every push/PR. This checks only these two literal strings, not "no phantom config key can ever exist" — a brand-new undocumented-in-code key would not be caught by this job (the job's second step separately checks that five output *type values* have a `case` in `buildWriter`, which is narrower still). `config.toml.example` was silently unscanned until a 2026-08-08 review found the `--include` bug (GNU grep applies `--include` to explicitly-named files, not just directory walks) and it was fixed and re-verified with a real appended key. `docs/adr/ADR-011-tls-certificate-automation.md` remains whole-file exempt from the `acme_staging` check (its Consequences section is the legitimate historical content); and, within this file, any other new row that happens to name both retired strings on one line is exempted the same way this row is, by design — the guard cannot tell a second legitimate worked example apart from a real reintroduction sharing this row's own exemption trigger | Verified |
| `make docs` succeeds under `--strict` (no broken internal links) | Development workflow, `CLAUDE.md` | `make docs` (run locally as part of this release's verification; not yet re-run by CI after mkdocs.yml's `copyright`/nav changes in this release, since `docs.yml` only triggers on push to `main`) | **Verified (manual)** — will also run automatically as `docs.yml`'s CI check on merge to `main` |
