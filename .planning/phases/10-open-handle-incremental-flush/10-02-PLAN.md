---
phase: 10-open-handle-incremental-flush
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - /Users/fjacquet/Projects/go-evtx/CHANGELOG.md
  - /Users/fjacquet/Projects/cee-exporter/go.mod
  - /Users/fjacquet/Projects/cee-exporter/go.sum
autonomous: true
requirements:
  - EVTX-01

must_haves:
  truths:
    - "go-evtx v0.3.0 is tagged and pushed to GitHub"
    - "cee-exporter go.mod replace directive points to local go-evtx (or published v0.3.0) and make test passes"
    - "CHANGELOG.md in go-evtx documents the Phase 10 open-handle changes under v0.3.0"
  artifacts:
    - path: "/Users/fjacquet/Projects/go-evtx/CHANGELOG.md"
      provides: "v0.3.0 release notes documenting open-handle model"
      contains: "v0.3.0"
    - path: "/Users/fjacquet/Projects/cee-exporter/go.mod"
      provides: "Updated go-evtx version reference"
      contains: "go-evtx"
  key_links:
    - from: "/Users/fjacquet/Projects/cee-exporter/go.mod"
      to: "/Users/fjacquet/Projects/go-evtx"
      via: "replace directive: github.com/fjacquet/go-evtx => ../go-evtx (or v0.3.0 tag)"
      pattern: "fjacquet/go-evtx"
---

<objective>
Tag go-evtx v0.3.0 and update cee-exporter's go.mod to reference the new version. Update CHANGELOG.md with v0.3.0 release notes.

Purpose: Complete the Phase 10 delivery by publishing the open-handle model as a named release so cee-exporter has a stable reference. v0.3.0 is a minor version bump (new feature: multi-chunk support; no breaking API change — RotationConfig, New, WriteRecord, WriteRaw, Close signatures are all unchanged).
Output: go-evtx v0.3.0 tagged on GitHub; cee-exporter go.mod updated; CHANGELOG.md updated; make test still green.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/fjacquet/Projects/cee-exporter/.planning/PROJECT.md
@/Users/fjacquet/Projects/cee-exporter/.planning/ROADMAP.md
@/Users/fjacquet/Projects/cee-exporter/.planning/phases/10-open-handle-incremental-flush/10-01-SUMMARY.md

<interfaces>
<!-- Current go.mod state from Phase 9 -->

From /Users/fjacquet/Projects/cee-exporter/go.mod (Phase 9 state):
```
github.com/fjacquet/go-evtx v0.2.0
...
replace github.com/fjacquet/go-evtx => ../go-evtx
```

Note: The replace directive means cee-exporter always uses the local go-evtx regardless of the version tag in require. After tagging v0.3.0, update the require line to v0.3.0 but keep the replace directive (developer workflow). This mirrors the Phase 9 pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CHANGELOG.md and tag go-evtx v0.3.0</name>
  <files>/Users/fjacquet/Projects/go-evtx/CHANGELOG.md</files>
  <action>
Read /Users/fjacquet/Projects/go-evtx/CHANGELOG.md to understand existing format.

Prepend a v0.3.0 section following the existing CHANGELOG format. Include:
- Version: v0.3.0
- Date: current date (2026-03-05)
- Type: Added/Changed/Fixed
- Key changes:
  - Added: `f *os.File` open-handle model — file created in `New()`, held open through `Close()`
  - Added: `flushChunkLocked()` — writes complete 65,536-byte chunks incrementally via `f.WriteAt`; patches file header after each chunk; calls `f.Sync()`
  - Added: `tickFlushLocked()` — goroutine tick writes partial chunk to disk without advancing chunk count (Option A flush-without-reset)
  - Added: Multi-chunk EVTX support — sessions exceeding ~2,400 events now write correctly (EVTX-01)
  - Changed: `WriteRecord()` and `WriteRaw()` now call `flushChunkLocked()` when buffer reaches 65,024 bytes (was warn-only)
  - Changed: `Close()` deletes placeholder file on empty session (zero-record backward compatibility)
  - Removed: `flushToFile()` — replaced by `flushChunkLocked()` + `tickFlushLocked()`

Then commit the CHANGELOG update and tag v0.3.0:
```
cd /Users/fjacquet/Projects/go-evtx
git add CHANGELOG.md evtx.go openhandle_test.go goroutine_test.go evtx_test.go
git commit -m "feat: open-handle incremental flush, multi-chunk support (EVTX-01) v0.3.0"
git tag v0.3.0
git push origin main
git push origin v0.3.0
```

Note: The commit may already exist from Plan 10-01 task commits. If so, just tag the current HEAD:
```
cd /Users/fjacquet/Projects/go-evtx
git tag v0.3.0
git push origin v0.3.0
```
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/go-evtx && git tag | grep v0.3.0</automated>
  </verify>
  <done>go-evtx v0.3.0 tag exists. CHANGELOG.md contains v0.3.0 section. `git tag | grep v0.3.0` returns `v0.3.0`.</done>
</task>

<task type="auto">
  <name>Task 2: Update cee-exporter go.mod to reference v0.3.0</name>
  <files>/Users/fjacquet/Projects/cee-exporter/go.mod, /Users/fjacquet/Projects/cee-exporter/go.sum</files>
  <action>
Update cee-exporter/go.mod require line from `github.com/fjacquet/go-evtx v0.2.0` to `github.com/fjacquet/go-evtx v0.3.0`. Keep the replace directive unchanged: `replace github.com/fjacquet/go-evtx => ../go-evtx`.

Run: `cd /Users/fjacquet/Projects/cee-exporter && go mod tidy`

This updates go.sum if needed. Then run: `make test` to confirm all packages pass.

Commit the go.mod and go.sum changes:
```
cd /Users/fjacquet/Projects/cee-exporter
git add go.mod go.sum
git commit -m "chore(deps): bump go-evtx to v0.3.0 (open-handle incremental flush)"
```
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cee-exporter && make test 2>&1 | tail -10</automated>
  </verify>
  <done>cee-exporter/go.mod shows `github.com/fjacquet/go-evtx v0.3.0`. `make test` passes all packages. go.sum is consistent.</done>
</task>

</tasks>

<verification>
Final verification checklist for plan 10-02:

1. `cd /Users/fjacquet/Projects/go-evtx && git tag | grep v0.3.0` returns `v0.3.0`
2. CHANGELOG.md has v0.3.0 section with open-handle changes documented
3. cee-exporter/go.mod has `github.com/fjacquet/go-evtx v0.3.0` in require block
4. replace directive still present: `replace github.com/fjacquet/go-evtx => ../go-evtx`
5. `cd /Users/fjacquet/Projects/cee-exporter && make test` — all packages pass
6. Both repos have clean git status after commits
</verification>

<success_criteria>
- go-evtx v0.3.0 tagged and pushed to GitHub
- cee-exporter go.mod updated to v0.3.0 (with replace directive preserved)
- make test in cee-exporter: all packages pass
- CHANGELOG.md documents the open-handle model changes
- Phase 10 EVTX-01 requirement fully delivered: sessions of any length write all events to disk
</success_criteria>

<output>
After completion, create `/Users/fjacquet/Projects/cee-exporter/.planning/phases/10-open-handle-incremental-flush/10-02-SUMMARY.md` with:
- go-evtx v0.3.0 tag confirmation
- cee-exporter go.mod version updated
- make test status
- Phase 10 completion statement: EVTX-01 satisfied
</output>
