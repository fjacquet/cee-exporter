---
phase: 01-quality
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/server/server.go
  - pkg/server/server_test.go
autonomous: true
requirements:
  - QUAL-05

must_haves:
  truths:
    - "readBody does not panic when a request body exceeds 64 MiB"
    - "readBody returns a non-nil error (not nil) when the body exceeds the limit"
    - "go test ./pkg/server/... passes with no failures on Linux"
  artifacts:
    - path: "pkg/server/server.go"
      provides: "Fixed readBody accepting http.ResponseWriter as first arg"
      contains: "func readBody(w http.ResponseWriter, r *http.Request)"
    - path: "pkg/server/server_test.go"
      provides: "Regression test proving the fix holds"
      contains: "TestReadBodyOversized"
  key_links:
    - from: "pkg/server/server.go"
      to: "http.MaxBytesReader"
      via: "readBody(w, r) call"
      pattern: "MaxBytesReader\\(w,"
    - from: "pkg/server/server.go ServeHTTP"
      to: "readBody"
      via: "updated call site"
      pattern: "readBody\\(w, r\\)"
---

<objective>
Fix the nil ResponseWriter panic in readBody and add a regression test.

Purpose: A payload exceeding 64 MiB triggers http.MaxBytesReader to close the connection via the ResponseWriter. Passing nil crashes the process — this is the only known panic in the codebase.
Output: Fixed server.go with updated readBody signature + server_test.go containing a regression test that proves no panic occurs.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pkg/server/server.go
@.planning/phases/01-quality/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix readBody nil ResponseWriter</name>
  <files>pkg/server/server.go</files>
  <action>
    In pkg/server/server.go, make exactly two changes:

    1. Change the readBody function signature from:
       `func readBody(r *http.Request) ([]byte, error)`
       to:
       `func readBody(w http.ResponseWriter, r *http.Request) ([]byte, error)`

    2. Inside readBody, change the MaxBytesReader call from:
       `r.Body = http.MaxBytesReader(nil, r.Body, maxBody)`
       to:
       `r.Body = http.MaxBytesReader(w, r.Body, maxBody)`

    3. Update the call site in ServeHTTP from:
       `body, err := readBody(r)`
       to:
       `body, err := readBody(w, r)`

    Do not change any other logic. Do not change any other function signature.
    The error-handling loop in readBody that checks `err.Error() == "EOF"` must remain unchanged.
  </action>
  <verify>go build ./pkg/server/... returns exit 0 with no output</verify>
  <done>go build ./pkg/server/... succeeds; readBody signature is func readBody(w http.ResponseWriter, r *http.Request) ([]byte, error); call site passes w as first argument</done>
</task>

<task type="auto">
  <name>Task 2: Write readBody regression test</name>
  <files>pkg/server/server_test.go</files>
  <action>
    Create pkg/server/server_test.go with package declaration `package server` (white-box, same package).

    Imports required:
    - "bytes"
    - "net/http"
    - "net/http/httptest"
    - "testing"

    Write two table-driven tests using t.Run:

    TestReadBodyOversized: Proves the fix — no panic, returns error.
      - Build a body of (64<<20)+1 bytes (64 MiB + 1 byte) using bytes.Repeat([]byte("x"), (64<<20)+1)
      - Create request: httptest.NewRequest(http.MethodPut, "/", bytes.NewReader(big))
      - Create recorder: httptest.NewRecorder()
      - Call readBody(rec, req) — must NOT panic, must return a non-nil error
      - If err == nil: t.Error("expected error for oversized body, got nil")

    TestReadBodyNormal: Proves normal payloads still work.
      - Build a 1 KiB body: bytes.Repeat([]byte("a"), 1024)
      - Create request and recorder the same way
      - Call readBody(rec, req) — must return no error and len(got) == 1024
      - If err != nil: t.Errorf("unexpected error: %v", err)
      - If len(got) != 1024: t.Errorf("body len = %d, want 1024", len(got))

    Do NOT use t.Parallel() in these tests (the function is pure; parallel is fine but unnecessary).
    Do NOT call NewHandler or ServeHTTP — test readBody directly.
    Do NOT import testify or any external library — stdlib only.
  </action>
  <verify>go test ./pkg/server/... -v -run TestReadBody outputs PASS for both subtests with no panic</verify>
  <done>go test ./pkg/server/... passes; TestReadBodyOversized returns error without panic; TestReadBodyNormal returns correct body bytes</done>
</task>

</tasks>

<verification>
Run: go test ./pkg/server/... -v -race

Expected output:
- --- PASS: TestReadBodyOversized
- --- PASS: TestReadBodyNormal
- No "panic" anywhere in output
- No "DATA RACE" anywhere in output
- Exit code 0
</verification>

<success_criteria>
- pkg/server/server.go compiles cleanly (go build ./...)
- readBody signature includes http.ResponseWriter as first parameter
- http.MaxBytesReader receives w (not nil)
- ServeHTTP call site updated to pass w
- go test ./pkg/server/... -race passes with no failures and no races
- TestReadBodyOversized confirms no panic on a 64 MiB + 1 body
</success_criteria>

<output>
After completion, create .planning/phases/01-quality/01-01-SUMMARY.md
</output>
