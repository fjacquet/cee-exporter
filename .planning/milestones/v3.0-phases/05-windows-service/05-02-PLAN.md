---
phase: 05-windows-service
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - cmd/cee-exporter/service_helpers.go
  - cmd/cee-exporter/service_helpers_test.go
autonomous: true
requirements:
  - DEPLOY-03

must_haves:
  truths:
    - "parseCfgPath correctly returns config path from -config or --config flag in args"
    - "parseCfgPath returns 'config.toml' default when no -config flag is present"
    - "parseCfgPath strips 'install'/'uninstall' subcommands — they do not affect the result"
    - "parseCfgPath has no build tag — it compiles and tests on Linux CI"
    - "go test ./cmd/cee-exporter/... passes with all parseCfgPath cases green"
  artifacts:
    - path: "cmd/cee-exporter/service_helpers.go"
      provides: "parseCfgPath(args []string) string pure function"
      contains: "func parseCfgPath"
    - path: "cmd/cee-exporter/service_helpers_test.go"
      provides: "table-driven tests for parseCfgPath"
      contains: "TestParseCfgPath"
  key_links:
    - from: "cmd/cee-exporter/service_windows.go"
      to: "cmd/cee-exporter/service_helpers.go"
      via: "service_windows.go calls parseCfgPath(os.Args[1:]) at install time"
      pattern: "parseCfgPath"
---

<objective>
TDD implementation of parseCfgPath — the pure function that extracts the -config path from os.Args so it can be stored in the SCM service registry at install time (preventing the "infinite install loop" pitfall from the research).

Purpose: When an operator runs `cee-exporter.exe install`, the service must record the correct config path in the Windows registry so SCM replays it on boot — NOT the "install" subcommand itself. parseCfgPath separates concerns cleanly and is 100% testable on Linux CI without any Win32 API surface.

Output: service_helpers.go (no build tag, pure Go), service_helpers_test.go (table-driven, stdlib only), all tests passing.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-windows-service/05-RESEARCH.md
</context>

<feature>
  <name>parseCfgPath — extract config file path from CLI args</name>
  <files>cmd/cee-exporter/service_helpers.go, cmd/cee-exporter/service_helpers_test.go</files>
  <behavior>
    parseCfgPath(args []string) string

    Takes os.Args[1:] (or equivalent slice). Returns the value of the first
    -config or --config flag found. Returns "config.toml" if not found.

    Test cases (table-driven with t.Run):
    - args=[]                                    → "config.toml"
    - args=["-config", "myapp.toml"]            → "myapp.toml"
    - args=["--config", "/etc/app.toml"]        → "/etc/app.toml"
    - args=["install", "-config", "prod.toml"]  → "prod.toml"  (subcommand ignored)
    - args=["-config"]                           → "config.toml"  (dangling flag, default)
    - args=["--verbose", "-config", "x.toml"]   → "x.toml"   (other flags ignored)
    - args=["uninstall"]                         → "config.toml"

    Test file declares package main (white-box, matches project convention).
    No external test dependencies (stdlib only per CLAUDE.md).
  </behavior>
  <implementation>
    service_helpers.go — no build tag, package main:

    ```go
    package main

    // parseCfgPath extracts the value of -config or --config from args
    // without calling flag.Parse(). Returns "config.toml" if not found.
    // Used by runWithServiceManager on Windows to store the config path in
    // the SCM registry at install time.
    func parseCfgPath(args []string) string {
        for i, a := range args {
            if (a == "-config" || a == "--config") && i+1 < len(args) {
                return args[i+1]
            }
        }
        return "config.toml"
    }
    ```

    RED-GREEN-REFACTOR:
    1. RED: Write TestParseCfgPath in service_helpers_test.go with all cases → run `go test ./cmd/cee-exporter/... -run TestParseCfgPath` → expect compile error (function undefined)
    2. Create service_helpers.go with parseCfgPath stub returning "" → run tests → expect FAIL (wrong return value)
    3. GREEN: Implement parseCfgPath correctly → run tests → expect PASS
    4. REFACTOR: Ensure no duplication; function is minimal and readable. Run tests again.
  </implementation>
</feature>

<verification>
1. `go test ./cmd/cee-exporter/... -run TestParseCfgPath -v` — all sub-tests PASS
2. `go test ./...` — full suite still passes (no regressions)
3. `grep "//go:build" cmd/cee-exporter/service_helpers.go` — returns nothing (no build tag, compiles on all platforms)
4. `grep "package main" cmd/cee-exporter/service_helpers_test.go` — white-box test package confirmed
</verification>

<success_criteria>

- service_helpers.go exists with parseCfgPath(args []string) string, no build tag
- service_helpers_test.go has TestParseCfgPath with at least 7 table-driven cases
- All cases pass including: default, -config flag, --config flag, subcommand prefix, dangling flag
- go test ./... passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/05-windows-service/05-02-SUMMARY.md`
</output>
