apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cee-exporter.fullname" . }}
  labels:
    {{- include "cee-exporter.labels" . | nindent 4 }}
  {{- with .Values.deploymentAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "cee-exporter.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "cee-exporter.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "cee-exporter.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - "-config"
            - "/etc/cee-exporter/config.toml"
          {{- with .Values.extraArgs }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            # CEPA TCP listener (PowerStore audit event receiver)
            - name: cepa
              containerPort: {{ .Values.service.port }}
              protocol: TCP
            # Prometheus /metrics endpoint (enabled via config [metrics] block)
            - name: metrics
              containerPort: {{ .Values.service.metricsPort }}
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if or (.Values.config) (.Values.existingSecret) (.Values.volumeMounts) }}
          volumeMounts:
          {{- end }}
            {{- if or .Values.config .Values.existingSecret }}
            - mountPath: "/etc/cee-exporter"
              name: config
              readOnly: true
            {{- end }}
          {{- range .Values.volumeMounts }}
            - {{ toYaml . | indent 14 | trim }}
          {{- end -}}
      {{- if or (.Values.config) (.Values.existingSecret) (.Values.volumes) }}
      volumes:
      {{- end }}
      {{- if or .Values.config .Values.existingSecret }}
        - name: config
          secret:
            secretName: {{ .Values.existingSecret | default (printf "%s-config" (include "cee-exporter.fullname" .)) }}
      {{- end }}
      {{- range .Values.volumes }}
        - {{ toYaml . | indent 10 | trim }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
