{{- if and (not .Values.existingSecret) .Values.config }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cee-exporter.fullname" . }}-config
  labels:
    {{- include "cee-exporter.labels" . | nindent 4 }}
stringData:
  config.toml: {{ tpl .Values.config . | quote }}
{{- end }}
